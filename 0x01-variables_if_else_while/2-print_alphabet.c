#include <stdio.h>
/**
 * main - Entry point
 * Return: 0
 * This code prints the alphabets in lowercase
 */
int main(void)
{
char ch = 'a';

for (ch = 'a'; ch <= 'z'; ch++)
	putchar(ch);
return (0);
}
